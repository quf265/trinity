shutdown(fd=280, how=0x55b9dd45e) = -1 (Socket operation on non-socket)
shutdown(fd=280, how=0x55b9dd45e) = -1 (Socket operation on non-socket)
shutdown(fd=280, how=0x55b9dd45e) = -1 (Socket operation on non-socket)
shutdown(fd=287, how=0x55b9dd45e) = -1 (Socket operation on non-socket)
shutdown(fd=287, how=0x55b9dd45e) = -1 (Socket operation on non-socket)
shutdown(fd=287, how=0x55b9dd45e) = -1 (Socket operation on non-socket)
shutdown(fd=287, how=0x55b9dd45e) = -1 (Socket operation on non-socket)
shutdown(fd=287, how=0x55b9dd45e) = -1 (Socket operation on non-socket)
shutdown(fd=287, how=0x55b9dd45e) = -1 (Socket operation on non-socket)
shutdown(fd=372, how=0x55b9dd45e) = -1 (Socket operation on non-socket)
shutdown(fd=372, how=0x55b9dd45e) = -1 (Socket operation on non-socket)
shutdown(fd=281, how=0x0) = -1 (Socket operation on non-socket)
shutdown(fd=281, how=0x0) = -1 (Socket operation on non-socket)
shutdown(fd=281, how=0x0) = -1 (Socket operation on non-socket)
shutdown(fd=281, how=0x0) = -1 (Socket operation on non-socket)
shutdown(fd=178, how=0x0) = -1 (Socket operation on non-socket)
shutdown(fd=178, how=0x0) = -1 (Socket operation on non-socket)
shutdown(fd=178, how=0x0) = -1 (Socket operation on non-socket)
shutdown(fd=118, how=0x0) = -1 (Socket operation on non-socket)
shutdown(fd=118, how=0x0) = -1 (Socket operation on non-socket)
shutdown(fd=118, how=0x0) = -1 (Socket operation on non-socket)
shutdown(fd=118, how=0x0) = -1 (Socket operation on non-socket)
shutdown(fd=335, how=0x35) = -1 (Invalid argument)
shutdown(fd=335, how=0x35) = -1 (Invalid argument)
shutdown(fd=335, how=0x35) = -1 (Invalid argument)
shutdown(fd=335, how=0x35) = -1 (Invalid argument)
shutdown(fd=275, how=0x35) = -1 (Socket operation on non-socket)
shutdown(fd=275, how=0x35) = -1 (Socket operation on non-socket)
shutdown(fd=275, how=0x35) = -1 (Socket operation on non-socket)
shutdown(fd=275, how=0x35) = -1 (Socket operation on non-socket)
shutdown(fd=275, how=0x35) = -1 (Socket operation on non-socket)
shutdown(fd=275, how=0x35) = -1 (Socket operation on non-socket)
shutdown(fd=277, how=0x35) = -1 (Socket operation on non-socket)
shutdown(fd=277, how=0x1) = -1 (Socket operation on non-socket)
shutdown(fd=277, how=0x1) = -1 (Socket operation on non-socket)
shutdown(fd=367, how=0x1) = -1 (Socket operation on non-socket)
shutdown(fd=367, how=0x1) = -1 (Socket operation on non-socket)
shutdown(fd=38, how=0x1) = -1 (Socket operation on non-socket)
shutdown(fd=38, how=0x1) = -1 (Socket operation on non-socket)
shutdown(fd=38, how=0x1) = -1 (Socket operation on non-socket)
shutdown(fd=19, how=0x1) = -1 (Socket operation on non-socket)
shutdown(fd=19, how=0x1) = -1 (Socket operation on non-socket)
shutdown(fd=19, how=0x1) = -1 (Socket operation on non-socket)
shutdown(fd=19, how=0x1) = -1 (Socket operation on non-socket)
shutdown(fd=355, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=355, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=355, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=355, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=355, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=293, how=0x2) = -1 (Transport endpoint is not connected)
shutdown(fd=293, how=0x2) = -1 (Transport endpoint is not connected)
shutdown(fd=293, how=0x2) = -1 (Transport endpoint is not connected)
shutdown(fd=281, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=281, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=281, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=356, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=356, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=356, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=356, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=356, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=280, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=280, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=280, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=280, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=273, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=273, how=0x2) = -1 (Socket operation on non-socket)
shutdown(fd=273, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=273, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=273, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=318, how=0x3) = -1 (Invalid argument)
shutdown(fd=318, how=0x3) = -1 (Invalid argument)
shutdown(fd=318, how=0x3) = -1 (Invalid argument)
shutdown(fd=318, how=0x3) = -1 (Invalid argument)
shutdown(fd=7, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=7, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=215, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=215, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=172, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=172, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=172, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=172, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=172, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=172, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=215, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=215, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=215, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=215, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=215, how=0x3) = -1 (Socket operation on non-socket)
shutdown(fd=215, how=0x0) = -1 (Socket operation on non-socket)
